import { RentalRequestEntity } from './rental-request.entity';

describe('RentalRequestEntity', () => {
  it('should be defined', () => {
    expect(new RentalRequestEntity()).toBeDefined();
  });
});
